apiVersion: v1
kind: Secret
metadata:
  name: acr-secret
  annotations:
    kubernetes.io/service-account.name: default
type: kubernetes.io/dockerconfigjson
data:
  .dockerconfigjson: << insert here genereted code >>
